#include <iostream>
#include <string>

using namespace std;

void print(string printertext, bool endline);
string readFile(string filePath);
int occurances(string s, char key);
int idOperator(string oper);

